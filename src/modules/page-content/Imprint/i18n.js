import { defineMessages } from 'react-intl';

const scope = 'imprint';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'An imprint is still missing...'
  },
  goBack: {
    id: `${scope}.goBack`,
    defaultMessage: 'Go back to the landing page'
  }
});
